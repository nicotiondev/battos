// Package main es el entrypoint del binario `battos`.
//
// En Fase 0 solo deja el módulo compilable.
// En Fase 1 se conectan los comandos cobra (status, project, agent, etc.).
package main

func main() {
	// placeholder — Fase 1 reemplaza esto con el root cobra command.
}
